import numpy as np

stats = np.array([[1, 2, 3], [4, 5, 6]])
print(stats, end = '\n\n')

# minimum and maximum element
print(np.min(stats), np.max(stats), end = '\n\n')

# sum of elements
print(np.sum(stats))