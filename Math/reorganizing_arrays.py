import numpy as np

# reshaping arrays
a = np.array([[1, 2, 3, 4], [5, 6, 7, 8]])
print(a.reshape(4, 2), end = '\n\n')

# vertically stacking vectors
v1 = np.array([1, 2, 3, 4])
v2 = np.array([5, 6, 7, 8])

